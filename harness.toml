# Harness default configuration

[llm]
provider = "anthropic"
model = "deepseek-v4-pro"
fallback_model = "deepseek-v4-flash"
api_key = "sk-0409535e41b1423a97051052413c9e34"       # 直接写在这里，优先于环境变量
api_base = "https://api.deepseek.com/anthropic"      # 自定义 API 端点（代理/私有部署）

[loop]
engine = "native"
max_turns = 30
compaction_threshold = 0.80

[sandbox]
runtime = "docker"

[repomap]
enabled = false

[cache]
warm_enabled = false

[observability]
backend = "none"
