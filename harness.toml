# Harness default configuration

[loop]
engine = "native"
max_turns = 30
compaction_threshold = 0.80

[sandbox]
runtime = "docker"

[repomap]
enabled = false

[cache]
warm_enabled = false

[observability]
backend = "none"
